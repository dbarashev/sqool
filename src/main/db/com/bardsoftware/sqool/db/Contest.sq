
CREATE TABLE TaskDto(
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    result_json TEXT NOT NULL
);

selectAllTasks:
SELECT * FROM TaskDto;

createNewTask:
INSERT INTO TaskDto(name, description, result_json) VALUES (?, ?, ?);

